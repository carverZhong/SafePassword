<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.SafePassword" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:activityOpenEnterAnimation">@anim/slide_in_right</item> <!-- 新Activity进入动画 -->
        <item name="android:activityOpenExitAnimation">@anim/slide_out_left</item>  <!-- 旧Activity退出动画 -->
        <item name="android:activityCloseEnterAnimation">@anim/slide_in_left</item>  <!-- 返回时旧Activity进入动画 -->
        <item name="android:activityCloseExitAnimation">@anim/slide_out_right</item> <!-- 返回时新Activity退出动画 -->
    </style>
</resources>